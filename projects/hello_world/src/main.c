#include <stdio.h>
/*
 * Title: Hello World
 * This is the most basic of programs.
 * 
 * Function: Main
 * Every C program should have a main function unless a userdefined __start function is defined.
 * This is the default entrypoint to any application written in C. This main is a simple function
 * that leverages the standard library for basic output to print the sentence "Hello world!"
 * 
 * Returns:
 *
 *  int
 */
 
int main() {

    printf("Hello, world!!!\n");   
    return 0;
}
